let rawip="IPアドレスがまだ取得されていないか、取得できない状態にあります。";function callback(json){console.log(json),rawip=json.ip}function Dainyu(){const oneline=`一行でいうと: <code>${platform.toString()}</code>`,ip=`IPアドレス: <code>${rawip}</code>`,accessfrom=`アクセス元ページ: <code>${location.href}</code>`,resolution=`解像度: <code>${screen.width} x ${screen.height}</code>`,browser=`ブラウザ: <code>${platform.name} ${platform.version}</code>`,engine=`HTMLレンダリングエンジン: <code>${platform.layout}</code>`,os=`OS: <code>${platform.os.toString()}</code>`,fordevua=`ユーザーエージェント（開発者向け）: <code>${platform.ua}</code>`;document.getElementById("1line").innerHTML=oneline,document.getElementById("ip").innerHTML=ip,document.getElementById("accessfrom").innerHTML=accessfrom,document.getElementById("resolution").innerHTML=resolution,document.getElementById("browser").innerHTML=browser,document.getElementById("os").innerHTML=os,document.getElementById("engine").innerHTML=engine,document.getElementById("fordevua").innerHTML=fordevua}window.addEventListener("load",Dainyu);